# Envio HyperIndex Environment Variables
# Copy this file to .env and fill in your actual values

# ============================================
# Required: Envio API Token
# ============================================
# API token for HyperSync access. Required from November 3, 2025.
# Generate your token at: https://envio.dev/app/api-keys
# For local development, this will be handled via automatic login from CLI
# For self-hosted deployments, this token is required
ENVIO_API_TOKEN=your_envio_api_token_here

# ============================================
# Optional: Custom RPC Endpoints
# ============================================
# Override default RPC endpoints for specific networks
# Recommended for production to ensure reliability
# Format: RPC_URL_<CHAIN_ID>=<your_rpc_url>

# Monad Mainnet (Chain ID: 143)
# RPC_URL_143=https://rpc-mainnet.monadinfra.com

# ============================================
# Optional: Database Configuration
# ============================================
# These are typically auto-managed by Envio but can be overridden
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DB=envio
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres

# ============================================
# Optional: Indexer Configuration
# ============================================
# LOG_LEVEL=info
# METRICS_PORT=9090

# ============================================
# Optional: External Read Controls
# ============================================
# Disable all eth_call-based reads (metadata, NFT balances)
# DISABLE_EXTERNAL_CALLS=true
# DISABLE_ETH_CALLS=true

# Enable one-time on-chain NFT ownership baselines during settlement (defaults to off)
# ENABLE_NFT_CHAIN_SYNC=true
